export default ({app, store}) => {
  // console.log('startup new')
  // const root = {root: true}
  // let fn = function () {
  //   if (store.state.auth.signingIn) {
  //     store.dispatch('auth/loadProfile', null, root)
  //   }
  // }
  // setInterval(fn, 2500)
}
